---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/vite.config.ts
  - web/tsconfig.json
  - web/tailwind.config.js
  - web/index.html
  - web/src/main.ts
  - web/src/App.vue
  - web/src/router/index.ts
  - web/src/views/UploadView.vue
  - web/src/components/VideoUploader.vue
  - web/src/components/AnalysisTypeSelector.vue
  - web/src/types/upload.ts
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
  - UPLD-04
must_haves:
  truths:
    - "Vue 3 + TypeScript + Vite 项目可以正常启动"
    - "用户可以拖拽或点击选择视频文件"
    - "上传过程显示进度条"
    - "上传完成后显示视频元信息（时长、分辨率、大小）"
    - "用户可以选择分析类型（正手、反手、发球、截击、全场综合）"
  artifacts:
    - path: "web/package.json"
      provides: "Vue 3 + Vite + TypeScript + Tailwind CSS 依赖配置"
      min_lines: 20
    - path: "web/src/views/UploadView.vue"
      provides: "上传页面，包含文件选择和元信息展示"
      min_lines: 100
    - path: "web/src/components/VideoUploader.vue"
      provides: "拖拽上传组件，支持进度显示"
      min_lines: 150
    - path: "web/src/components/AnalysisTypeSelector.vue"
      provides: "分析类型选择器（5种类型）"
      min_lines: 80
    - path: "web/src/types/upload.ts"
      provides: "TypeScript 类型定义"
      exports: ["UploadStatus", "VideoInfo", "AnalysisType"]
  key_links:
    - from: "web/src/views/UploadView.vue"
      to: "web/src/components/VideoUploader.vue"
      via: "组件引用"
      pattern: "<VideoUploader"
    - from: "web/src/views/UploadView.vue"
      to: "web/src/components/AnalysisTypeSelector.vue"
      via: "组件引用"
      pattern: "<AnalysisTypeSelector"
---

<objective>
初始化 Vue 3 + TypeScript + Vite 前端项目，配置 Tailwind CSS，创建视频上传界面组件。

Purpose: 搭建现代前端开发环境，实现用户友好的视频上传界面，支持拖拽上传、进度显示、元信息展示和分析类型选择。
Output: 可运行的 Vue 项目，包含完整的上传页面 UI。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

## Existing Codebase Context

- 项目根目录: `/Users/max/OneDrive/03-Coding/52-ai-sports-coach/`
- Python 后端已存在: `coach.py`, `tennis_annotate.py`, `src/` 目录
- 前端将创建在 `web/` 子目录中

## Tech Stack Decisions

- Vue 3 Composition API + TypeScript
- Vite 作为构建工具（快速，原生 ESM）
- Tailwind CSS 作为样式方案
- Vue Router 4 进行前端路由

## Upload Requirements Reference

- **UPLD-01**: 支持视频格式 mp4/mov/avi/mkv，拖拽或选择上传
- **UPLD-02**: 上传进度条显示
- **UPLD-03**: 显示视频时长、分辨率、文件大小
- **UPLD-04**: 分析类型选择（正手/反手/发球/截击/全场综合）
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vue 3 + TypeScript Project</name>
  <files>web/package.json, web/vite.config.ts, web/tsconfig.json, web/index.html, web/tailwind.config.js, web/postcss.config.js</files>
  <action>
    1. 在项目根目录运行 `npm create vue@latest web` 创建 Vue 项目，选项选择：
       - TypeScript: Yes
       - JSX Support: No
       - Vue Router: Yes
       - Pinia: No
       - Vitest: No
       - Cypress: No
       - ESLint: Yes (Prettier)
    
    2. 安装 Tailwind CSS 及相关依赖:
       ```bash
       cd web
       npm install -D tailwindcss postcss autoprefixer
       npx tailwindcss init -p
       ```
    
    3. 配置 `tailwind.config.js`:
       ```javascript
       /** @type {import('tailwindcss').Config} */
       export default {
         content: ['./index.html', './src/**/*.{vue,js,ts,jsx,tsx}'],
         theme: {
           extend: {},
         },
         plugins: [],
       }
       ```
    
    4. 创建 `src/style.css` 导入 Tailwind:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```
    
    5. 在 `main.ts` 中导入样式文件
  </action>
  <verify>
    <automated>cd /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web && npm install && npm run dev -- --host &</automated>
    <automated>curl -s http://localhost:5173 | grep -q "Vue" && echo "Vue dev server running"</automated>
  </verify>
  <done>
    - `web/package.json` 存在且包含 vue@3, vite, typescript, tailwindcss 依赖
    - `npm run dev` 可以启动开发服务器
    - 访问 localhost:5173 显示 Vue 默认页面
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Upload Page and Components</name>
  <files>web/src/types/upload.ts, web/src/components/VideoUploader.vue, web/src/components/AnalysisTypeSelector.vue, web/src/views/UploadView.vue, web/src/router/index.ts, web/src/App.vue</files>
  <action>
    1. 创建类型定义文件 `src/types/upload.ts`:
       ```typescript
       export type UploadStatus = 'idle' | 'uploading' | 'success' | 'error'
       
       export interface VideoInfo {
         file: File
         name: string
         size: number
         duration?: number
         width?: number
         height?: number
       }
       
       export type AnalysisType = 'forehand' | 'backhand' | 'serve' | 'volley' | 'full'
       
       export interface AnalysisTypeOption {
         value: AnalysisType
         label: string
         icon: string
         description: string
       }
       ```
    
    2. 创建 `src/components/VideoUploader.vue` - 拖拽上传组件：
       - 支持拖拽文件到区域
       - 支持点击选择文件
       - 验证文件类型（mp4/mov/avi/mkv/webm）
       - 显示上传进度条
       - 使用 HTML5 FileReader 提取视频元信息（通过创建隐藏的 video 元素获取 duration/width/height）
    
    3. 创建 `src/components/AnalysisTypeSelector.vue` - 分析类型选择器：
       - 5 种分析类型：正手(forehand)、反手(backhand)、发球(serve)、截击(volley)、全场综合(full)
       - 卡片式布局，显示图标和描述
       - 单选模式，默认选中"全场综合"
    
    4. 创建 `src/views/UploadView.vue` - 上传页面：
       - 页面标题和说明
       - 整合 VideoUploader 组件
       - 整合 AnalysisTypeSelector 组件
       - 显示已选择视频的信息（时长、分辨率、大小）
       - 提交按钮（先实现 UI，后端 API 在 Plan 1-02 中连接）
    
    5. 更新路由 `src/router/index.ts`，设置 UploadView 为首页
    
    6. 更新 `App.vue`，添加基础布局
  </action>
  <verify>
    <automated>ls -la /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/components/ | grep -E "VideoUploader|AnalysisTypeSelector"</automated>
    <automated>ls -la /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/views/ | grep UploadView</automated>
    <automated>grep -r "AnalysisType" /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/types/</automated>
  </verify>
  <done>
    - VideoUploader.vue 存在，支持拖拽和点击选择文件
    - AnalysisTypeSelector.vue 存在，包含5种分析类型选项
    - UploadView.vue 存在，整合两个组件
    - 类型定义文件包含 UploadStatus、VideoInfo、AnalysisType
    - 访问首页显示上传界面
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Upload UI Functionality</name>
  <what-built>
    Vue 前端项目已初始化，包含：
    1. 拖拽上传组件（VideoUploader）- 支持拖拽文件到上传区域或点击选择
    2. 进度条显示 - 上传过程中的视觉反馈
    3. 视频元信息显示 - 时长、分辨率、文件大小
    4. 分析类型选择器 - 5种分析类型的卡片式选择界面
  </what-built>
  <how-to-verify>
    1. 确保开发服务器正在运行：`cd web && npm run dev`
    2. 打开浏览器访问 http://localhost:5173
    3. 测试拖拽功能：将一个 mp4 视频文件拖拽到上传区域
    4. 测试点击选择：点击上传区域，选择视频文件
    5. 验证元信息显示：上传后应显示视频时长、分辨率、文件大小
    6. 验证分析类型选择：点击不同的分析类型卡片，确认可以切换选择
    7. 检查响应式布局：调整浏览器窗口大小，确认布局正常
  </how-to-verify>
  <resume-signal>验证通过请输入 "approved"，如有问题请描述</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Local Dev Server | 本地开发环境，无外部不信任输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | File upload | accept | 本地开发环境，文件仅在本地处理 |
| T-01-02 | Information Disclosure | Video metadata | accept | 本地运行，不涉及敏感数据传输 |
</threat_model>

<verification>
## Overall Verification

1. **项目结构验证**:
   ```bash
   ls /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/
   # 应包含: package.json, vite.config.ts, src/, index.html
   ```

2. **依赖安装验证**:
   ```bash
   cd /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web && npm list vue vite typescript tailwindcss
   ```

3. **开发服务器验证**:
   ```bash
   curl -s http://localhost:5173 | head -20
   ```

4. **组件文件验证**:
   ```bash
   ls /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/components/
   # 应包含: VideoUploader.vue, AnalysisTypeSelector.vue
   ```
</verification>

<success_criteria>
1. Vue 3 + TypeScript + Vite 项目成功初始化
2. Tailwind CSS 配置完成并生效
3. 开发服务器可以正常启动（npm run dev）
4. VideoUploader 组件支持拖拽和点击上传
5. 上传过程显示进度条
6. 上传后显示视频元信息（时长、分辨率、大小）
7. AnalysisTypeSelector 组件显示5种分析类型选项
8. 上传页面 UI 布局美观，响应式正常
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
