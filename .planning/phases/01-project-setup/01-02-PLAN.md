---
phase: 01-project-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/main.py
  - api/routers/upload.py
  - api/models/upload.py
  - api/services/video_service.py
  - api/requirements.txt
  - web/src/api/upload.ts
  - web/src/composables/useUpload.ts
  - web/.env.development
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
  - UPLD-04
must_haves:
  truths:
    - "FastAPI 后端服务可以正常启动"
    - "前端可以通过 API 上传视频文件"
    - "后端保存上传的视频到 output/uploads/ 目录"
    - "后端提取并返回视频元信息（时长、分辨率、大小）"
    - "后端接收并保存分析类型参数"
  artifacts:
    - path: "api/main.py"
      provides: "FastAPI 应用入口"
      min_lines: 30
    - path: "api/routers/upload.py"
      provides: "上传 API 路由"
      exports: ["router"]
    - path: "api/models/upload.py"
      provides: "上传请求/响应模型"
      exports: ["UploadResponse", "VideoMetadata"]
    - path: "api/services/video_service.py"
      provides: "视频处理服务"
      exports: ["extract_video_metadata", "save_uploaded_video"]
    - path: "web/src/api/upload.ts"
      provides: "前端上传 API 客户端"
      exports: ["uploadVideo"]
    - path: "web/src/composables/useUpload.ts"
      provides: "上传逻辑组合式函数"
      exports: ["useUpload"]
  key_links:
    - from: "web/src/composables/useUpload.ts"
      to: "web/src/api/upload.ts"
      via: "API 调用"
      pattern: "import.*from.*api/upload"
    - from: "web/src/api/upload.ts"
      to: "http://localhost:8000/api/upload"
      via: "HTTP POST"
      pattern: "axios.post|fetch.*api/upload"
    - from: "api/routers/upload.py"
      to: "output/uploads/"
      via: "文件保存"
      pattern: "open.*uploads|shutil.copy"
    - from: "api/services/video_service.py"
      to: "ffmpeg"
      via: "视频元信息提取"
      pattern: "subprocess.*ffmpeg"
---

<objective>
搭建 FastAPI 后端服务，实现视频上传 API，集成前端上传功能。

Purpose: 提供后端 API 支持视频文件上传、元信息提取，并与前端上传界面打通。
Output: 可运行的 FastAPI 服务，前端可以上传视频并接收元信息响应。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@coach.py
@requirements.txt

## Existing Codebase Context

- Python 环境已配置，使用 Python 3.11+
- 现有依赖见 `requirements.txt`（openai, requests, python-dotenv 等）
- 项目使用 ffmpeg 进行视频处理（已在 coach.py 中使用）
- 输出目录结构：`output/` 用于存储分析结果

## Backend Structure

```
api/
├── main.py              # FastAPI 入口
├── requirements.txt     # Python 依赖
├── routers/
│   └── upload.py        # 上传路由
├── models/
│   └── upload.py        # Pydantic 模型
└── services/
    └── video_service.py # 视频处理服务
```

## API Design

- POST `/api/upload` - 上传视频
  - Request: multipart/form-data (file, analysis_type)
  - Response: `{video_id, filename, metadata: {duration, width, height, size}}`

## Integration Points

- 前端通过 `axios` 或原生 `fetch` 调用后端 API
- 使用 `FormData` 构造上传请求
- 通过 `XMLHttpRequest` 或 `axios` onUploadProgress 实现进度监听
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set Up FastAPI Backend Structure</name>
  <files>api/requirements.txt, api/main.py, api/routers/upload.py, api/models/upload.py, api/services/video_service.py, api/__init__.py</files>
  <action>
    1. 创建 `api/requirements.txt`，添加 FastAPI 相关依赖：
       ```
       fastapi>=0.110.0
       uvicorn[standard]>=0.29.0
       python-multipart>=0.0.9
       pydantic>=2.6.0
       ```
    
    2. 创建 `api/main.py` - FastAPI 应用入口：
       ```python
       from fastapi import FastAPI
       from fastapi.middleware.cors import CORSMiddleware
       from routers import upload
       
       app = FastAPI(title="AI Tennis Coach API", version="1.0.0")
       
       # CORS 配置，允许前端访问
       app.add_middleware(
           CORSMiddleware,
           allow_origins=["http://localhost:5173"],  # Vue dev server
           allow_credentials=True,
           allow_methods=["*"],
           allow_headers=["*"],
       )
       
       app.include_router(upload.router, prefix="/api", tags=["upload"])
       
       @app.get("/health")
       def health_check():
           return {"status": "ok"}
       ```
    
    3. 创建 `api/models/upload.py` - Pydantic 模型：
       ```python
       from pydantic import BaseModel
       from typing import Literal
       
       AnalysisType = Literal["forehand", "backhand", "serve", "volley", "full"]
       
       class VideoMetadata(BaseModel):
           duration: float  # seconds
           width: int
           height: int
           size: int  # bytes
           format: str
       
       class UploadResponse(BaseModel):
           video_id: str
           filename: str
           analysis_type: AnalysisType
           metadata: VideoMetadata
           message: str
       ```
    
    4. 创建 `api/routers/upload.py` - 上传路由：
       - POST /upload 端点
       - 接收文件和分析类型
       - 调用 video_service 保存文件和提取元信息
       - 返回 UploadResponse
       - 确保 output/uploads/ 目录存在
    
    5. 创建 `api/services/video_service.py` - 视频处理服务：
       ```python
       import subprocess
       import json
       from pathlib import Path
       from typing import Tuple
       
       UPLOAD_DIR = Path("output/uploads")
       UPLOAD_DIR.mkdir(parents=True, exist_ok=True)
       
       def extract_video_metadata(file_path: Path) -> Tuple[float, int, int]:
           """使用 ffprobe 提取视频元信息"""
           cmd = [
               "ffprobe", "-v", "error",
               "-select_streams", "v:0",
               "-show_entries", "stream=width,height,duration",
               "-of", "json",
               str(file_path)
           ]
           result = subprocess.run(cmd, capture_output=True, text=True)
           data = json.loads(result.stdout)
           stream = data["streams"][0]
           return (
               float(stream.get("duration", 0)),
               stream.get("width", 0),
               stream.get("height", 0)
           )
       
       def save_uploaded_video(file_content: bytes, filename: str, video_id: str) -> Path:
           """保存上传的视频文件"""
           file_path = UPLOAD_DIR / f"{video_id}_{filename}"
           file_path.write_bytes(file_content)
           return file_path
       ```
    
    6. 创建 `api/routers/__init__.py` 和 `api/services/__init__.py`
  </action>
  <verify>
    <automated>cd /Users/max/OneDrive/03-Coding/52-ai-sports-coach/api && pip install -r requirements.txt</automated>
    <automated>cd /Users/max/OneDrive/03-Coding/52-ai-sports-coach/api && python -c "import main; print('FastAPI imports OK')"</automated>
    <automated>test -f /Users/max/OneDrive/03-Coding/52-ai-sports-coach/api/main.py && echo "main.py exists"</automated>
    <automated>test -f /Users/max/OneDrive/03-Coding/52-ai-sports-coach/api/routers/upload.py && echo "upload router exists"</automated>
  </verify>
  <done>
    - api/requirements.txt 包含 fastapi, uvicorn, python-multipart, pydantic
    - api/main.py 创建 FastAPI 应用，配置 CORS
    - api/models/upload.py 定义 UploadResponse 和 VideoMetadata 模型
    - api/routers/upload.py 创建上传路由
    - api/services/video_service.py 实现视频元信息提取和文件保存
    - Python 依赖安装成功
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Frontend with Backend API</name>
  <files>web/src/api/upload.ts, web/src/composables/useUpload.ts, web/.env.development, web/package.json, web/src/components/VideoUploader.vue, web/src/views/UploadView.vue</files>
  <action>
    1. 安装前端 HTTP 客户端（如果尚未安装）：
       ```bash
       cd web && npm install axios
       ```
    
    2. 创建 `web/.env.development` 配置 API 地址：
       ```
       VITE_API_BASE_URL=http://localhost:8000
       ```
    
    3. 创建 `web/src/api/upload.ts` - API 客户端：
       ```typescript
       import axios from 'axios'
       import type { VideoInfo, AnalysisType } from '@/types/upload'
       
       const API_BASE = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'
       
       export interface UploadResult {
         video_id: string
         filename: string
         analysis_type: AnalysisType
         metadata: {
           duration: number
           width: number
           height: number
           size: number
           format: string
         }
         message: string
       }
       
       export async function uploadVideo(
         file: File,
         analysisType: AnalysisType,
         onProgress: (percent: number) => void
       ): Promise<UploadResult> {
         const formData = new FormData()
         formData.append('file', file)
         formData.append('analysis_type', analysisType)
       
         const response = await axios.post(`${API_BASE}/api/upload`, formData, {
           headers: { 'Content-Type': 'multipart/form-data' },
           onUploadProgress: (progressEvent) => {
             const percent = progressEvent.total
               ? Math.round((progressEvent.loaded * 100) / progressEvent.total)
               : 0
             onProgress(percent)
           }
         })
       
         return response.data
       }
       ```
    
    4. 创建 `web/src/composables/useUpload.ts` - 上传逻辑组合式函数：
       ```typescript
       import { ref } from 'vue'
       import { uploadVideo, type UploadResult } from '@/api/upload'
       import type { AnalysisType } from '@/types/upload'
       
       export function useUpload() {
         const uploading = ref(false)
         const progress = ref(0)
         const error = ref<string | null>(null)
         const result = ref<UploadResult | null>(null)
       
         async function upload(
           file: File,
           analysisType: AnalysisType
         ): Promise<UploadResult | null> {
           uploading.value = true
           progress.value = 0
           error.value = null
           result.value = null
       
           try {
             const data = await uploadVideo(file, analysisType, (percent) => {
               progress.value = percent
             })
             result.value = data
             return data
           } catch (err) {
             error.value = err instanceof Error ? err.message : 'Upload failed'
             return null
           } finally {
             uploading.value = false
           }
       
         return {
           uploading,
           progress,
           error,
           result,
           upload
         }
       }
       ```
    
    5. 更新 `web/src/components/VideoUploader.vue`：
       - 添加 submit 功能，接收 analysisType prop
       - 使用 useUpload composable 处理上传
       - 上传成功后显示结果或跳转
    
    6. 更新 `web/src/views/UploadView.vue`：
       - 管理选中的分析类型状态
       - 传递给 VideoUploader
       - 上传成功后跳转到分析等待页面（Phase 2 实现，暂时显示成功消息）
  </action>
  <verify>
    <automated>grep -r "uploadVideo" /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/api/</automated>
    <automated>grep -r "useUpload" /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/composables/</automated>
    <automated>grep -r "VITE_API_BASE_URL" /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/.env.development</automated>
  </verify>
  <done>
    - web/src/api/upload.ts 创建，包含 uploadVideo 函数和 UploadResult 类型
    - web/src/composables/useUpload.ts 创建，封装上传状态管理
    - web/.env.development 配置 API 基础 URL
    - VideoUploader 组件集成上传逻辑
    - 前端可以调用后端 API 上传视频
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify End-to-End Upload Flow</name>
  <what-built>
    FastAPI 后端已搭建，包含：
    1. POST /api/upload 端点 - 接收视频文件和分析类型
    2. 视频元信息提取 - 使用 ffprobe 获取时长、分辨率
    3. 文件保存 - 保存到 output/uploads/ 目录
    4. CORS 配置 - 允许前端访问
    5. 前端 API 集成 - axios 调用后端，支持进度监听

    端到端流程：
    1. 前端选择视频文件和分析类型
    2. 点击上传，显示进度条
    3. 文件发送到后端 API
    4. 后端保存文件并提取元信息
    5. 返回视频 ID 和元信息
  </what-built>
  <how-to-verify>
    1. 启动后端服务：
       ```bash
       cd /Users/max/OneDrive/03-Coding/52-ai-sports-coach/api
       uvicorn main:app --reload --port 8000
       ```
    2. 确认后端运行：访问 http://localhost:8000/health 应返回 {"status": "ok"}
    3. 确认前端运行：`cd web && npm run dev`
    4. 打开 http://localhost:5173，选择一个测试视频文件（mp4/mov/avi/mkv）
    5. 选择分析类型（如"正手"）
    6. 点击上传按钮
    7. 验证：
       - 进度条从 0% 增长到 100%
       - 上传完成后显示视频元信息
       - 后端控制台显示保存的文件信息
       - 检查 output/uploads/ 目录是否生成文件
    8. 测试文件：可以使用任意视频文件，或使用命令生成测试视频：
       ```bash
       ffmpeg -f lavfi -i testsrc=duration=10:size=640x480:rate=30 -pix_fmt yuv420p test.mp4
       ```
  </how-to-verify>
  <resume-signal>验证通过请输入 "approved"，如有问题请描述</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend ↔ Backend API | 本地开发环境，CORS 限制为 localhost |
| Backend ↔ File System | 写入本地 output/uploads/ 目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | File upload | accept | 本地开发环境，仅接受已知视频格式 |
| T-01-04 | Tampering | Upload request | accept | 本地运行，无敏感数据 |
| T-01-05 | Repudiation | File storage | accept | 本地使用，无需审计日志 |
| T-01-06 | Information Disclosure | File path | accept | 本地路径，不涉及敏感信息 |
| T-01-07 | Denial of Service | Large file upload | mitigate | 限制上传文件大小（50MB），在后续阶段实现 |
</threat_model>

<verification>
## Overall Verification

1. **后端服务启动验证**:
   ```bash
   cd /Users/max/OneDrive/03-Coding/52-ai-sports-coach/api
   uvicorn main:app --port 8000 &
   curl http://localhost:8000/health
   # 应返回 {"status": "ok"}
   ```

2. **API 端点验证**:
   ```bash
   curl -X POST http://localhost:8000/api/upload \
     -F "file=@test.mp4" \
     -F "analysis_type=forehand"
   # 应返回视频元信息 JSON
   ```

3. **文件存储验证**:
   ```bash
   ls -la /Users/max/OneDrive/03-Coding/52-ai-sports-coach/output/uploads/
   # 应包含上传的视频文件
   ```

4. **前端 API 集成验证**:
   ```bash
   grep -r "uploadVideo" /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/
   grep -r "useUpload" /Users/max/OneDrive/03-Coding/52-ai-sports-coach/web/src/
   ```

5. **端到端验证**:
   - 前端选择文件并上传
   - 后端接收并保存文件
   - 返回元信息并在前端显示
</verification>

<success_criteria>
1. FastAPI 后端服务可以正常启动（uvicorn main:app）
2. /health 端点返回状态正常
3. POST /api/upload 端点接收文件和分析类型参数
4. 上传的视频保存到 output/uploads/ 目录
5. 使用 ffprobe 成功提取视频时长、分辨率信息
6. 前端通过 axios 调用后端 API，支持进度监听
7. useUpload composable 封装上传状态和错误处理
8. 端到端上传流程可以正常工作（前端→后端→文件保存）
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>
