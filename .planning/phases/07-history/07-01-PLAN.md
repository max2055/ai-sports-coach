---
phase: 07-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/models/history.py
  - api/services/history_service.py
  - api/routers/history.py
  - api/main.py
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
must_haves:
  truths:
    - "后端可以列出所有历史分析记录"
    - "支持按视频名搜索"
    - "支持按分析类型筛选"
    - "可以删除历史记录及关联文件"
    - "可以重新查看历史报告"
  artifacts:
    - path: "api/models/history.py"
      provides: "历史数据模型"
      exports: ["HistoryEntry"]
    - path: "api/services/history_service.py"
      provides: "历史管理服务"
      exports: ["list_history", "get_history_entry", "delete_history_entry"]
    - path: "api/routers/history.py"
      provides: "历史 API 路由"
      exports: ["router"]
---

<objective>
创建后端历史管理服务，扫描 output/analysis/ 目录下的分析记录，提供搜索、筛选、删除功能。

Purpose: 为用户提供分析历史的管理能力。
Output: GET /api/history, DELETE /api/history/{video_id}, GET /api/history/{video_id}
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

## 数据来源
- output/analysis/{video_id}/status.json - 分析状态（包含 video_id, status, analysis_type, started_at, completed_at）
- output/analysis/{video_id}/summary.json - 汇总分析（包含 overall_score）
- output/uploads/ - 原始视频文件
- report/tennis/coach_*.md - 教练报告（旧版）

## HistoryEntry 数据结构
- video_id: str
- filename: str (原始文件名)
- analysis_type: str (forehand/backhand/serve/volley/full)
- status: str (completed/failed/pending)
- created_at: datetime (从 status.json started_at)
- completed_at: datetime (可选)
- overall_score: int (从 summary.json 提取，若无则为 None)
- duration: float (视频时长，从 status.json 或 metadata)

## 删除逻辑
- 删除 output/analysis/{video_id}/ 整个目录
- 删除 output/uploads/{video_id}_* 视频文件（如果存在）
- 删除关联的 coach_*.md 报告（如果存在）

## 搜索/筛选
- 按视频名搜索（模糊匹配，不区分大小写）
- 按分析类型筛选
- 按状态筛选（completed/failed）
- 按时间范围筛选（可选）
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create History Data Models</name>
  <files>api/models/history.py</files>
  <action>
    创建 `api/models/history.py`:
    ```python
    from pydantic import BaseModel
    from typing import Literal, Optional
    from datetime import datetime

    class HistoryEntry(BaseModel):
        video_id: str
        filename: str
        analysis_type: str
        status: Literal["completed", "failed", "pending"]
        created_at: datetime
        completed_at: datetime | None = None
        overall_score: int | None = None
        duration: float | None = None

    class HistoryList(BaseModel):
        entries: list[HistoryEntry]
        total: int
    ```
  </action>
  <verify>
    grep -q "HistoryEntry" api/models/history.py
    grep -q "HistoryList" api/models/history.py
  </verify>
  <done>
    - history.py 包含 HistoryEntry 和 HistoryList 模型
  </done>
</task>

<task type="auto">
  <name>Task 2: Create History Service</name>
  <files>api/services/history_service.py</files>
  <action>
    创建 `api/services/history_service.py`:

    核心函数:

    1. list_history(search=None, analysis_type=None, status_filter=None) → HistoryList
       - 扫描 output/analysis/ 下所有子目录
       - 读取每个目录的 status.json
       - 读取 summary.json 获取 overall_score
       - 尝试从 filename 获取原始文件名
       - 按 started_at 倒序排列（最新的在前）
       - 应用筛选条件

    2. get_history_entry(video_id) → HistoryEntry
       - 读取特定视频的分析状态
       - 返回完整信息

    3. delete_history_entry(video_id) → bool
       - 删除 output/analysis/{video_id}/ 整个目录
       - 尝试删除 output/uploads/{video_id}_* 文件
       - 返回是否成功

    4. get_video_filename(video_id) → str
       - 从 status.json 或目录结构推断原始文件名

    文件路径:
    - ANALYSIS_DIR = Path("output/analysis")
    - UPLOAD_DIR = Path("output/uploads")
    - REPORT_DIR = Path("report/tennis")
  </action>
  <verify>
    grep -q "list_history" api/services/history_service.py
    grep -q "delete_history_entry" api/services/history_service.py
  </verify>
  <done>
    - history_service.py 包含历史管理的核心函数
  </done>
</task>

<task type="auto">
  <name>Task 3: Create History Router</name>
  <files>api/routers/history.py, api/main.py</files>
  <action>
    1. 创建 `api/routers/history.py`:
       ```python
       from fastapi import APIRouter, HTTPException
       from models.history import HistoryEntry, HistoryList
       from services.history_service import list_history, get_history_entry, delete_history_entry

       router = APIRouter()

       @router.get("/history", response_model=HistoryList)
       async def get_history(
           search: str | None = None,
           analysis_type: str | None = None,
           status_filter: str | None = None,
       ):
           return list_history(search=search, analysis_type=analysis_type, status_filter=status_filter)

       @router.get("/history/{video_id}", response_model=HistoryEntry)
       async def get_history_item(video_id: str):
           entry = get_history_entry(video_id)
           if not entry:
               raise HTTPException(status_code=404, detail=f"History entry '{video_id}' not found")
           return entry

       @router.delete("/history/{video_id}")
       async def delete_history_item(video_id: str):
           success = delete_history_entry(video_id)
           if not success:
               raise HTTPException(status_code=404, detail=f"History entry '{video_id}' not found")
           return {"message": f"History entry '{video_id}' deleted"}
       ```

    2. 更新 `api/main.py`:
       - 导入 history router
       - app.include_router(history.router, prefix="/api", tags=["history"])
  </action>
  <verify>
    grep -q "get_history" api/routers/history.py
    grep -q "delete_history_item" api/routers/history.py
    grep -q "include_router.*history" api/main.py
  </verify>
  <done>
    - GET /api/history 返回历史列表，支持搜索/筛选
    - GET /api/history/{video_id} 返回单条记录
    - DELETE /api/history/{video_id} 删除记录及关联文件
    - main.py 包含 history router
  </done>
</task>

</tasks>

<verification>
- [ ] api/models/history.py 存在
- [ ] api/services/history_service.py 包含 list_history 和 delete_history_entry
- [ ] api/routers/history.py 包含 GET/DELETE /api/history 端点
- [ ] main.py 包含 history router
</verification>

<success_criteria>
- 后端可以列出所有历史分析记录
- 支持按视频名搜索和分析类型筛选
- 可以删除历史记录及关联文件
</success_criteria>

<output>
After completion, create `.planning/phases/07-history/07-01-SUMMARY.md`
</output>