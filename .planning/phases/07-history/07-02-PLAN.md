---
phase: 07-history
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - web/src/api/history.ts
  - web/src/views/HistoryView.vue
  - web/src/router/index.ts
  - web/src/components/HistoryList.vue
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
must_haves:
  truths:
    - "前端有独立的历史页面（HistoryView）"
    - "显示列表：日期、视频名、分析类型、评分"
    - "搜索框支持按视频名搜索"
    - "筛选下拉框按分析类型筛选"
    - "删除按钮带确认对话框"
    - "点击条目跳转到报告页"
  artifacts:
    - path: "web/src/views/HistoryView.vue"
      provides: "历史页面视图"
      min_lines: 120
    - path: "web/src/api/history.ts"
      provides: "历史 API 客户端"
      exports: ["getHistory", "deleteHistoryEntry"]
    - path: "web/src/components/HistoryList.vue"
      provides: "历史记录列表组件"
      min_lines: 80
---

<objective>
创建前端历史页面，显示分析历史列表，支持搜索、筛选、删除和重新查看。

Purpose: 用户可以管理和回顾所有历史分析记录。
Output: HistoryView 页面 + HistoryList 组件 + 路由配置。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@web/src/router/index.ts

## Phase 7 Plan 01 已完成
- GET /api/history 返回历史列表，支持 search, analysis_type, status_filter 参数
- DELETE /api/history/{video_id} 删除记录
- GET /api/history/{video_id} 返回单条记录

## 页面布局
- 顶部: 搜索框 + 筛选下拉框
- 主体: 历史记录列表（表格或卡片形式）
- 每条记录: 日期、视频名、分析类型、评分（如果有）、状态
- 操作: 删除按钮（带确认对话框）、点击查看报告

## 分析类型中文映射
- forehand: 正手
- backhand: 反手
- serve: 发球
- volley: 截击
- full: 全场综合

## 状态映射
- completed: 已完成（绿色）
- failed: 失败（红色）
- pending: 分析中（蓝色）

## 路由
- /history → HistoryView
- 点击条目 → /report/{video_id}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create History API Client</name>
  <files>web/src/api/history.ts</files>
  <action>
    创建 `web/src/api/history.ts`:
    ```typescript
    import axios from 'axios'

    const API_BASE = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'

    export interface HistoryEntry {
      video_id: string
      filename: string
      analysis_type: string
      status: 'completed' | 'failed' | 'pending'
      created_at: string
      completed_at?: string
      overall_score?: number
      duration?: number
    }

    export interface HistoryList {
      entries: HistoryEntry[]
      total: number
    }

    export async function getHistory(params?: {
      search?: string
      analysis_type?: string
      status_filter?: string
    }): Promise<HistoryList> {
      const response = await axios.get<HistoryList>(`${API_BASE}/api/history`, { params })
      return response.data
    }

    export async function deleteHistoryEntry(videoId: string): Promise<void> {
      await axios.delete(`${API_BASE}/api/history/${videoId}`)
    }
    ```
  </action>
  <verify>
    grep -q "getHistory" web/src/api/history.ts
    grep -q "deleteHistoryEntry" web/src/api/history.ts
  </verify>
  <done>
    - history.ts 包含历史 API 客户端
  </done>
</task>

<task type="auto">
  <name>Task 2: Create History List Component</name>
  <files>web/src/components/HistoryList.vue</files>
  <action>
    创建 `web/src/components/HistoryList.vue`:

    - Props: entries (HistoryEntry[])
    - Emit: select(videoId), delete(videoId)
    - 表格/卡片布局（响应式）
    - 每条记录显示:
      * 日期（created_at 格式化）
      * 文件名
      * 分析类型（中文映射）
      * 评分（overall_score，有则显示，带颜色）
      * 状态标签
      * 操作按钮: 查看报告、删除
    - 空列表时显示提示
  </action>
  <verify>
    grep -q "HistoryEntry" web/src/components/HistoryList.vue
    grep -q "emit.*delete" web/src/components/HistoryList.vue
  </verify>
  <done>
    - HistoryList 显示历史记录列表
    - 支持选择和删除操作
  </done>
</task>

<task type="auto">
  <name>Task 3: Create History View and Router</name>
  <files>web/src/views/HistoryView.vue, web/src/router/index.ts</files>
  <action>
    1. 创建 `web/src/views/HistoryView.vue`:
       - 调用 getHistory() 获取列表
       - 搜索框 (v-model searchQuery)
       - 分析类型筛选下拉框
       - 使用 HistoryList 组件显示
       - 删除确认对话框 (window.confirm)
       - 跳转到 /report/{video_id} 重新查看

       结构:
       ```vue
       <template>
         <div class="min-h-screen bg-gray-50 dark:bg-gray-900">
           <!-- Header -->
           <div class="bg-white dark:bg-gray-800 shadow-sm">
             <h1 class="text-2xl font-bold">分析历史</h1>
           </div>

           <!-- Search and Filter -->
           <div class="max-w-7xl mx-auto px-4 py-4 flex gap-4">
             <input v-model="searchQuery" placeholder="搜索视频名..." />
             <select v-model="selectedType">
               <option value="">全部类型</option>
               <option value="forehand">正手</option>
               <option value="backhand">反手</option>
               <option value="serve">发球</option>
               <option value="volley">截击</option>
               <option value="full">全场综合</option>
             </select>
           </div>

           <!-- List -->
           <HistoryList
             :entries="filteredEntries"
             @select="viewReport"
             @delete="confirmDelete"
           />
         </div>
       </template>
       ```

    2. 更新 `web/src/router/index.ts`:
       - 添加 /history 路由 → HistoryView
  </action>
  <verify>
    grep -q "HistoryView" web/src/views/HistoryView.vue
    grep -q "history" web/src/router/index.ts
  </verify>
  <done>
    - HistoryView 包含搜索、筛选、列表、删除功能
    - 路由 /history 已配置
  </done>
</task>

</tasks>

<verification>
- [ ] web/src/api/history.ts 存在
- [ ] HistoryView.vue 包含搜索、筛选、列表
- [ ] HistoryList.vue 存在
- [ ] router/index.ts 包含 /history 路由
</verification>

<success_criteria>
- 前端有独立历史页面
- 显示列表：日期、视频名、分析类型、评分
- 搜索和筛选功能可用
- 删除带确认对话框
- 点击条目跳转到报告页
</success_criteria>

<output>
After completion, create `.planning/phases/07-history/07-02-SUMMARY.md`
</output>