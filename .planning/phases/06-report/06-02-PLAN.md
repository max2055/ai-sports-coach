---
phase: 06-report
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - web/src/api/report.ts
  - web/src/components/ScoreCard.vue
  - web/src/components/StrengthsList.vue
  - web/src/components/IssueAnalysis.vue
  - web/src/components/TrainingPlan.vue
  - web/src/components/CoachSummary.vue
  - web/src/views/ReportView.vue
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06
must_haves:
  truths:
    - "前端可以显示综合评分（总分 + 6 维度分）"
    - "显示亮点列表（2-3 个）"
    - "显示问题深度分析（每个问题一页）"
    - "显示问题汇总表"
    - "显示专项训练计划"
    - "显示教练总结"
  artifacts:
    - path: "web/src/components/ScoreCard.vue"
      provides: "综合评分卡片（总分 + 6 维度雷达）"
      min_lines: 100
    - path: "web/src/components/StrengthsList.vue"
      provides: "亮点展示列表"
      min_lines: 60
    - path: "web/src/components/IssueAnalysis.vue"
      provides: "问题深度分析组件"
      min_lines: 120
    - path: "web/src/components/TrainingPlan.vue"
      provides: "专项训练计划展示"
      min_lines: 80
    - path: "web/src/components/CoachSummary.vue"
      provides: "教练总结展示"
      min_lines: 60
    - path: "web/src/api/report.ts"
      provides: "报告 API 客户端"
      exports: ["getCoachReport"]
---

<objective>
创建前端报告页面和组件，展示综合评分、亮点、问题分析、训练计划和教练总结。

Purpose: 为用户提供结构化的教练报告阅读体验。
Output: 完整的报告页面，包含评分、亮点、问题分析、训练计划、总结。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@web/src/views/ReportView.vue
@web/src/components/ConsistencyRadar.vue

## Phase 6 Plan 01 已完成
- GET /api/report/{video_id} 返回结构化报告
- 包含 overall_score, radar_scores, strengths, issues, improvement_suggestions, issue_summary, coach_summary, training_plan

## 信息层次设计
1. 综合评分卡片（顶部，总分醒目 + 6 维度雷达图）
2. 亮点列表（简洁，2-3 个，绿色标记）
3. 问题深度分析（每个问题展开页，红色标记，含帧引用）
4. 问题汇总表（表格形式，帧/类型/部位/描述/优先级）
5. 专项训练计划（可执行建议）
6. 教练总结（段落文本）

## 评分可视化
- 总分: 大号数字，颜色随分数变化（≥7 绿色，5-6 黄色，<5 红色）
- 6 维度: 复用 ConsistencyRadar 组件
- 亮点: 绿色打勾列表
- 问题: 红色感叹号 + 展开面板
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Report API Client and Types</name>
  <files>web/src/api/report.ts</files>
  <action>
    创建 `web/src/api/report.ts`:
    ```typescript
    import axios from 'axios'

    const API_BASE = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'

    export interface Strength {
      text: string
      frame_refs: string[]
    }

    export interface Issue {
      text: string
      frame_refs: string[]
      severity: 'high' | 'medium' | 'low'
    }

    export interface ImprovementSuggestion {
      text: string
      number: number
    }

    export interface IssueSummaryRow {
      frame_number: number
      issue_type: string
      body_part: string
      description: string
      priority: 'high' | 'medium' | 'low'
    }

    export interface RadarScores {
      hitting_technique: number
      footwork: number
      body_rotation: number
      timing: number
      fitness: number
      tactics: number
    }

    export interface CoachReport {
      video_id: string
      overall_score: number
      radar_scores: RadarScores
      strengths: Strength[]
      issues: Issue[]
      improvement_suggestions: ImprovementSuggestion[]
      issue_summary: IssueSummaryRow[]
      coach_summary: string
      training_plan: string
    }

    export async function getCoachReport(videoId: string): Promise<CoachReport> {
      const response = await axios.get<CoachReport>(`${API_BASE}/api/report/${videoId}`)
      return response.data
    }
    ```
  </action>
  <verify>
    grep -q "getCoachReport" web/src/api/report.ts
    grep -q "CoachReport" web/src/api/report.ts
  </verify>
  <done>
    - report.ts 包含报告类型定义和 API 函数
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Score Card Component</name>
  <files>web/src/components/ScoreCard.vue</files>
  <action>
    创建 `web/src/components/ScoreCard.vue`:

    功能:
    - Props: overallScore, radarScores
    - 左侧: 大号总分数字（颜色随分数变化）
    - 右侧: 6 维度评分条（水平进度条形式）
    - 维度名称中文: 击球技术、步法、身体旋转、击球节奏、体能、战术

    评分颜色:
    - ≥7: 绿色 (text-green-600)
    - 5-6: 黄色 (text-yellow-600)
    - <5: 红色 (text-red-600)

    实现:
    - 使用 computed 获取分数颜色类
    - 6 维度用 v-for 循环显示进度条
    - 每个维度显示名称和分数 (N/10)
  </action>
  <verify>
    grep -q "overallScore" web/src/components/ScoreCard.vue
    grep -q "radarScores" web/src/components/ScoreCard.vue
  </verify>
  <done>
    - ScoreCard 显示总分和 6 维度评分
    - 分数颜色随等级变化
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Strengths, Issues, and Summary Components</name>
  <files>web/src/components/StrengthsList.vue, web/src/components/IssueAnalysis.vue, web/src/components/CoachSummary.vue</files>
  <action>
    1. 创建 `web/src/components/StrengthsList.vue`:
       - Props: strengths (Strength[])
       - 绿色打勾列表
       - 每个亮点显示文本和帧引用
       - 帧引用显示为小标签

    2. 创建 `web/src/components/IssueAnalysis.vue`:
       - Props: issues (Issue[])
       - 每个问题可展开（details/summary 或 accordion）
       - 问题按 severity 排序（high 在前）
       - 每个问题显示:
         * 问题文本（红色标记）
         * severity 标签（high=红，medium=橙，low=黄）
         * 关联帧引用（标签形式）
       - 底部显示改进建议列表

    3. 创建 `web/src/components/CoachSummary.vue`:
       - Props: summary (string)
       - 段落文本显示
       - 带背景高亮（浅蓝色背景）
  </action>
  <verify>
    grep -q "strengths" web/src/components/StrengthsList.vue
    grep -q "issues" web/src/components/IssueAnalysis.vue
    grep -q "summary" web/src/components/CoachSummary.vue
  </verify>
  <done>
    - StrengthsList 显示亮点列表
    - IssueAnalysis 显示可展开的问题分析
    - CoachSummary 显示教练总结
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Training Plan Component</name>
  <files>web/src/components/TrainingPlan.vue</files>
  <action>
    创建 `web/src/components/TrainingPlan.vue`:

    功能:
    - Props: trainingPlan (string)
    - 显示训练计划文本
    - 如果有编号项目，格式化为有序列表
    - 带图标装饰（训练相关图标）

    实现:
    - 按换行符分割训练计划
    - 检测编号项目（1. 2. 3. 或 - 开头）
    - 格式化为 ol/ul 列表
    - 其余文本作为段落显示
  </action>
  <verify>
    grep -q "trainingPlan" web/src/components/TrainingPlan.vue
  </verify>
  <done>
    - TrainingPlan 显示结构化的训练计划
  </done>
</task>

<task type="auto">
  <name>Task 5: Integrate into ReportView</name>
  <files>web/src/views/ReportView.vue</files>
  <action>
    更新 `web/src/views/ReportView.vue`:
    - 在视频和图表区域上方添加报告头部
    - 调用 getCoachReport(videoId) 获取报告数据
    - 布局顺序:
      1. ScoreCard（综合评分卡片）
      2. StrengthsList（亮点列表）
      3. IssueAnalysis（问题分析）
      4. IssueSummaryTable（问题汇总表 - 用简单表格，不需独立组件）
      5. TrainingPlan（训练计划）
      6. CoachSummary（教练总结）
    - 然后显示现有的视频播放器、时间轴、图表区域

    报告区域放在最顶部，让用户先看到评分和分析结果，再查看视频和图表细节。
  </action>
  <verify>
    grep -q "ScoreCard" web/src/views/ReportView.vue
    grep -q "getCoachReport" web/src/views/ReportView.vue
  </verify>
  <done>
    - ReportView 包含完整的报告展示区域
    - 报告区域在视频和图表之前
  </done>
</task>

</tasks>

<verification>
- [ ] web/src/api/report.ts 存在
- [ ] ScoreCard.vue 显示总分和 6 维度
- [ ] StrengthsList.vue 显示亮点列表
- [ ] IssueAnalysis.vue 显示可展开的问题分析
- [ ] TrainingPlan.vue 显示训练计划
- [ ] CoachSummary.vue 显示教练总结
- [ ] ReportView.vue 包含报告区域
</verification>

<success_criteria>
- 前端可以显示综合评分（总分 + 6 维度分）
- 显示亮点列表（2-3 个）
- 显示问题深度分析（每个问题一页）
- 显示问题汇总表
- 显示专项训练计划
- 显示教练总结
</success_criteria>

<output>
After completion, create `.planning/phases/06-report/06-02-SUMMARY.md`
</output>