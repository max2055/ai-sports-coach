---
phase: 06-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/models/report.py
  - api/services/report_service.py
  - api/routers/report.py
  - api/main.py
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06
must_haves:
  truths:
    - "后端可以解析 coach_*.md 报告"
    - "提取综合评分、亮点、问题、训练计划、教练总结"
    - "API 返回结构化 JSON"
  artifacts:
    - path: "api/models/report.py"
      provides: "报告数据模型"
      exports: ["CoachReport", "CoachReportSection"]
    - path: "api/services/report_service.py"
      provides: "报告解析服务"
      exports: ["parse_coach_report", "get_report_for_video"]
    - path: "api/routers/report.py"
      provides: "报告 API 路由"
      exports: ["router"]
---

<objective>
创建后端报告解析服务，从 coach_*.md 中提取结构化数据供前端报告页面使用。

Purpose: 将 Markdown 格式的教练报告转化为前端可渲染的 JSON 结构。
Output: GET /api/report/{video_id} 返回结构化报告数据。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@report/tennis/coach_20260319_204910.md

## Coach Report 结构（参考现有 coach_*.md）
```markdown
# Coach Analysis: 网球 — 2026-03-19

## Overall Score: 6/10

## Strengths
- 引拍时机较好（Frame 1, Frame 2）
- 保持了不错的重心平衡（Frame 3, Frame 6）
- 击球后有基本的恢复（Frame 8, Frame 12）

## Issues Found
- 髋肩旋转不足，影响击球力量（Frame 5）
- 击球点未在身体的最佳位置（Frame 6）
- 随挥动作不够完整且自然（Frame 10）
- 步法移动较慢，准备不充分（Frame 11）

## Improvement Suggestions
1. 加强髋肩旋转练习，可以增加训练中的模拟转体动作
2. 调整击球点，让球拍在身体稍前方接触球
3. 练习完整的随挥动作，确保在击球后自然流畅
4. 加强脚下步法练习，提高移动速度

## Coach Summary
该选手在引拍时机和重心平衡上表现不错...

## Standard Reference
![Reference: ref_001](references/ref_001.jpg)
```

## 报告存储位置
- report/tennis/coach_*.md — 教练分析报告
- output/analysis/{video_id}/summary.json — 汇总分析（Phase 2 产出）
- output/analysis/{video_id}/frames.json — 帧标注数据（Phase 2 产出）

## 解析策略
1. 优先从 output/analysis/{video_id}/summary.json 读取（如果存在）
2. 回退到解析 report/tennis/coach_*.md（最新文件）
3. 结合 frames.json 统计数据补充问题汇总表
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Report Data Models</name>
  <files>api/models/report.py</files>
  <action>
    创建 `api/models/report.py`:
    ```python
    from pydantic import BaseModel
    from typing import Literal

    class Strength(BaseModel):
        text: str
        frame_refs: list[str]  # ["Frame 1", "Frame 2"]

    class Issue(BaseModel):
        text: str
        frame_refs: list[str]
        severity: Literal["high", "medium", "low"] = "medium"

    class ImprovementSuggestion(BaseModel):
        text: str
        number: int

    class IssueSummaryRow(BaseModel):
        frame_number: int
        issue_type: str
        body_part: str
        description: str
        priority: Literal["high", "medium", "low"]

    class RadarScores(BaseModel):
        hitting_technique: int  # 1-10
        footwork: int
        body_rotation: int
        timing: int
        fitness: int
        tactics: int

    class CoachReport(BaseModel):
        video_id: str
        overall_score: int  # 1-10
        radar_scores: RadarScores
        strengths: list[Strength]
        issues: list[Issue]
        improvement_suggestions: list[ImprovementSuggestion]
        issue_summary: list[IssueSummaryRow]
        coach_summary: str
        training_plan: str  # 专项训练计划
    ```
  </action>
  <verify>
    grep -q "CoachReport" api/models/report.py
    grep -q "RadarScores" api/models/report.py
  </verify>
  <done>
    - report.py 包含所有报告数据模型
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Report Service</name>
  <files>api/services/report_service.py</files>
  <action>
    创建 `api/services/report_service.py`:

    核心函数:
    1. parse_coach_report(markdown_path) → CoachReport
       - 解析 Overall Score: N/10
       - 解析 Strengths 列表（提取 frame refs）
       - 解析 Issues Found 列表（提取 frame refs，按出现次数定 severity）
       - 解析 Improvement Suggestions（编号列表）
       - 解析 Coach Summary（段落文本）
       - 使用正则/Markdown 解析

    2. extract_issue_summary(frames_json_path) → list[IssueSummaryRow]
       - 从 frames.json 提取有问题帧
       - 映射 issue_type 到 body_part
       - 按出现频率排序

    3. generate_training_plan(issues, radar_scores) → str
       - 根据问题类型和评分生成训练计划
       - 如果 summary.json 中有训练计划直接使用
       - 否则基于问题类型生成建议

    4. get_report_for_video(video_id) → CoachReport
       - 查找对应的 coach_*.md 文件
       - 查找对应的 frames.json
       - 调用上述函数生成完整报告
       - 回退到 frames.json 统计数据

    解析规则:
    - Frame refs: 正则 r"Frame\s+(\d+)" 提取
    - Severity: issue 出现次数 >2 次 = high, 1 次 = medium, 0 = low
    - Radar scores: 从 summary.json 读取，若无则默认 5
  </action>
  <verify>
    grep -q "parse_coach_report" api/services/report_service.py
    grep -q "get_report_for_video" api/services/report_service.py
  </verify>
  <done>
    - report_service.py 包含报告解析和生成函数
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Report Router</name>
  <files>api/routers/report.py, api/main.py</files>
  <action>
    1. 创建 `api/routers/report.py`:
       ```python
       from fastapi import APIRouter, HTTPException
       from models.report import CoachReport
       from services.report_service import get_report_for_video

       router = APIRouter()

       @router.get("/report/{video_id}", response_model=CoachReport)
       async def get_report(video_id: str):
           report = get_report_for_video(video_id)
           if not report:
               raise HTTPException(status_code=404, detail=f"Report for video '{video_id}' not found")
           return report
       ```

    2. 更新 `api/main.py`:
       - 导入 report router
       - app.include_router(report.router, prefix="/api", tags=["report"])
  </action>
  <verify>
    grep -q "get_report" api/routers/report.py
    grep -q "include_router.*report" api/main.py
  </verify>
  <done>
    - GET /api/report/{video_id} 返回结构化报告
    - main.py 包含 report router
  </done>
</task>

</tasks>

<verification>
- [ ] api/models/report.py 存在
- [ ] api/services/report_service.py 包含 parse_coach_report 和 get_report_for_video
- [ ] api/routers/report.py 包含 GET /api/report/{video_id} 端点
- [ ] main.py 包含 report router
</verification>

<success_criteria>
- 后端可以返回结构化报告数据
- 包含评分、亮点、问题、训练计划、教练总结
- API 返回 JSON 格式
</success_criteria>

<output>
After completion, create `.planning/phases/06-report/06-01-SUMMARY.md`
</output>