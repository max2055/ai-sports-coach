---
phase: 02-analysis-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - web/src/api/analysis.ts
  - web/src/composables/useAnalysis.ts
  - web/src/views/AnalysisView.vue
  - web/src/router/index.ts
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03
  - PIPE-04
must_haves:
  truths:
    - "前端可以启动分析并显示状态"
    - "轮询显示进度（pending/analyzing/completed/failed）"
    - "分析完成后自动跳转到报告页"
    - "分析失败时显示错误信息和重试按钮"
  artifacts:
    - path: "web/src/api/analysis.ts"
      provides: "分析 API 客户端"
      exports: ["startAnalysis", "getAnalysisStatus"]
    - path: "web/src/composables/useAnalysis.ts"
      provides: "分析状态管理 composable"
      exports: ["useAnalysis"]
    - path: "web/src/views/AnalysisView.vue"
      provides: "分析等待页面，显示进度"
      min_lines: 80
---

<objective>
创建前端分析页面，实现状态轮询、进度显示、完成跳转和错误处理。

Purpose: 让用户看到分析进度，完成后自动跳转到结果页面。
Output: 完整的分析等待体验，支持重试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@web/src/views/UploadView.vue
@web/src/router/index.ts

## Phase 2 Plan 01 已完成
- POST /api/analyze/{video_id} 启动分析
- GET /api/status/{video_id} 查询状态
- 状态: pending, extracting, analyzing, annotating, completed, failed

## Phase 1 UploadView 已有
- 上传成功后获得 video_id
- 点击"开始分析"跳转到 /analysis/{videoId}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Analysis API Client</name>
  <files>web/src/api/analysis.ts, web/src/composables/useAnalysis.ts</files>
  <action>
    1. 创建 `web/src/api/analysis.ts`:
       ```typescript
       import axios from 'axios'

       const API_BASE = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'

       export interface AnalysisState {
         video_id: string
         status: 'pending' | 'extracting' | 'analyzing' | 'annotating' | 'completed' | 'failed'
         progress: number
         error?: string
         result_path?: string
       }

       export async function startAnalysis(videoId: string): Promise<{ video_id: string; status: string }> {
         const response = await axios.post(`${API_BASE}/api/analyze/${videoId}`)
         return response.data
       }

       export async function getAnalysisStatus(videoId: string): Promise<AnalysisState> {
         const response = await axios.get(`${API_BASE}/api/status/${videoId}`)
         return response.data
       }
       ```

    2. 创建 `web/src/composables/useAnalysis.ts`:
       - useAnalysis(videoId) composable
       - 状态 ref: status, progress, error
       - start() 函数启动分析
       - pollStatus() 函数每 2 秒轮询状态
       - 直到 completed 或 failed 停止轮询
  </action>
  <verify>
    grep -q "startAnalysis" web/src/api/analysis.ts
    grep -q "useAnalysis" web/src/composables/useAnalysis.ts
  </verify>
  <done>
    - analysis.ts 包含 startAnalysis 和 getAnalysisStatus
    - useAnalysis.ts 包含轮询逻辑
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Analysis View Page</name>
  <files>web/src/views/AnalysisView.vue, web/src/router/index.ts</files>
  <action>
    1. 创建 `web/src/views/AnalysisView.vue`:
       - 接收 route.params.videoId
       - 使用 useAnalysis 管理状态
       - 显示进度条和状态文字
       - 状态映射中文: pending="等待中", extracting="提取帧", analyzing="AI分析中", annotating="标注帧", completed="完成", failed="失败"
       - completed 时自动 router.push('/report/{videoId}')
       - failed 时显示错误信息和"重试"按钮

    2. 更新 `web/src/router/index.ts`:
       - 添加 { path: '/analysis/:videoId', name: 'analysis', component: AnalysisView }
       - 添加 { path: '/report/:videoId', name: 'report', component: () => import('../views/ReportView.vue') }
       - ReportView 暂时显示占位符（Phase 3 实现）
  </action>
  <verify>
    grep -q "AnalysisView" web/src/views/AnalysisView.vue
    grep -q "'analysis'" web/src/router/index.ts
  </verify>
  <done>
    - AnalysisView 页面显示进度
    - 完成后跳转到报告页
    - 失败时显示重试选项
</done>
</task>

</tasks>

<verification>
- [ ] web/src/api/analysis.ts 存在
- [ ] web/src/composables/useAnalysis.ts 存在并包含轮询逻辑
- [ ] web/src/views/AnalysisView.vue 显示进度条
- [ ] router 包含 /analysis/:videoId 和 /report/:videoId 路由
</verification>

<success_criteria>
- 前端可以启动分析并轮询状态
- 进度条显示百分比
- 完成后自动跳转
- 失败时显示错误信息和重试按钮
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-pipeline/02-02-SUMMARY.md`
</output>